# Mode: matrix

Use when the user wants a literature matrix for a saved query.

Behavior:

- ensure articles are indexed in the local SQLite database
- reuse article summary JSON when available
- export Markdown and CSV matrix files

Inputs:

- saved search query
