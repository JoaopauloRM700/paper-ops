# Mode: digest

Goal:

- reuse or build article summaries for one saved query
- synthesize those summaries into a cross-paper digest

Output expectations:

- summary prerequisites from `summarize`
- consolidated digest JSON under `output/digests/`
- consolidated digest markdown under `reports/digests/`
