# Mode: batch

Process `batch/batch-input.tsv` where each line contains an ID and a search query.

Expected format:

```tsv
id	query
1	("systematic review" AND rag)
2	("knowledge graph" AND screening)
```
